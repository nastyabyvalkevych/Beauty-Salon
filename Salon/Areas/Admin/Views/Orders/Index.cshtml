@model IEnumerable<OrderModel>

@*@{


    var status = Context.Request.Query["status"];

    var approved = "text-primary";
    var pending = "text-primary";
    var all = "text-primary";

    if (status == "approved")
    {
        approved = "active text-white";
    }
    else if (status == "pending")
    {
        pending = "active text-white";
    }
    else
    {
        all = "active text-white";
    }
}*@

<style>
    .bord1{
        margin: -7px 118px -34px 20px;
        padding: 10px 0px 20px 103px;
    }
    h1{
         text-transform: uppercase;
        font-family: 'Cormorant';
        font-size: 35px;
        text-align: center;
        color: black;
        margin:20px;
    }

    .all {
        padding: 60px;
        background-color: #FCF6FB;
        border-radius: 30px;
    }
</style>
<h1>
    Клієнти/Записи
</h1>
@*<form method="get" asp-action="Index">
    <p>
        <input type="search" placeholder="enter name" value="@ViewData[" GetEmployeedetails"]" name="date" style="width:500px"/>
        <input type="submit" value="Search" class="btn btn-primary"/>
        <a asp-action="Index">Get all employee</a>
    </p>

</form>*@
<div class="all">

<div class="bord1">
    <div class="d-flex justify-content-between mb3">
        @*<div class="p-2">
            <h2 class="text-primary">@ViewData["Title"]</h2>
        </div>*@
       @* <div class="p2">
            <ul class="list1">
                <a style="text-decoration:none" asp-controller="Orders" asp-action="Index" asp-route-status="pending">
                <li class="list-group-item @pending">Переглянуті</li>
                </a>
                <a style="text-decoration:none" asp-controller="Orders" asp-action="Index" asp-route-status="approved">
                <li class="list-group-item @approved">Підтверджені</li>
                </a>
                <a style="text-decoration:none" asp-controller="Orders" asp-action="Index" asp-route-status="all">
                <li class="list-group-item @all">Всі записи</li>
                </a>
            </ul>
        </div>*@
 @*   </div>
    <br /><br />*@
    <table id="myTable" class="table table-striped table-bordered table-hover w-100">
        <thead>
            <tr>
                <th>Ім'я клієнта</th>
                <th>Номер телефону</th>
                <th>Майстер</th>
                <th>Час</th>
                <th>Статус</th>
                
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td><a  asp-action="Details" asp-route-id="@item.Id">
                            @item.Name
                        </a></td>
                    <td>@item.PhoneNumber</td>
                    <td>@item.Employee</td>
                    <td>@item.OrderDate</td>
                    <td>@item.Status</td>
               
                </tr>
            }
        </tbody>
    </table>
</div>
    </div>
